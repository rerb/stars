# STARS ETL for the STARS Benchmarking Tool

Extracts and transforms data for the BT.

# Management command

Set `BT_HOST` environment variable... defaults to
`https: // stars-bt-stage.herokuapp.com`.

    python manage.py run_etl
